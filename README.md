# pyqusim
This repo contains a toy package for simulated quantum computing in numpy. 
It was created around 2023 to learn about quantum computing and play around. 
The original idea was to implement Shors algorithm, but it never got that far. 

The `example.py` shows how to entangle two qubits!
